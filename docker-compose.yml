version: '3.8'
services:
  ptv-trmnl:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - trmnl-data:/app/data
    restart: unless-stopped

volumes:
  trmnl-data:
